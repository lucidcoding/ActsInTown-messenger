import Conversation = require('../models/conversation.model');
import StartConversationRequest = require('../requests/conversation/start.conversation.request')

export function start(request: StartConversationRequest, callback: Function) {
    
}
